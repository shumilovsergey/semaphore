# RouterOS — не Linux: нет sudo, нет python, нет шелла.
ansible_user: ansible
ansible_connection: ssh
ansible_become: false

# Что забираем с роутера. Без ведущего слеша: в RouterOS путь — это имя
# из `/file print`, а там каталог называется просто "backup".
backup_src: flash/backup
