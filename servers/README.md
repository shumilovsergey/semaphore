[<- Назад ](../README.md)

## Серверы — `servers/`

Один файл = один сервер. Имя файла совпадает с именем хоста в inventory.

`1_default.yml` — особый файл, запускается на группе или конкретном хосте и хранит в себе дефолтную насройку серверов. Требует передать `target` через Extra Variables в Semaphore.

## Добавить новый сервер

Скопируй `0_server_template.yml`, переименуй в `<hostname>.yml`, замени `<hostname>` внутри файла на имя сервера, раскомментируй нужные роли и заполни их переменные.

## Preflight

Первая роль в каждом серверном файле — `preflight`.

Она запускается до всего остального и выполняет набор проверок. Если проверки прошли — выполнение продолжается. Если что-то не так — скрипт останавливается с описанием проблемы до того, как будут внесены какие-либо изменения на сервере.

Что именно проверяет `preflight` — смотри в `roles/preflight/tasks/main.yml`.
