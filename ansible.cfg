[defaults]

# чтобы путь к roles/, подхватывался автоматически
roles_path = roles

# когда output большой - проще взять его из логов чем из UI
log_path = /var/log/ansible.log

# маркер завершения прогона
callback_plugins = callback_plugins
callbacks_enabled = run_marker
