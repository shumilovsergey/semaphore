- name: Create node_exporter directory
  file:
    path: "{{ node_exporter_path }}"
    state: directory
    mode: "0755"

- name: Check if service already exists
  stat:
    path: "{{ service_file_path }}"
  register: service_file

- name: Fail if port is already in use (first run only)
  wait_for:
    host: localhost
    port: "{{ node_exporter_port }}"
    state: stopped
    timeout: 3
  when: not service_file.stat.exists

- name: Create binary cache directory on the controller
  file:
    path: "{{ node_exporter_cache_dir }}"
    state: directory
    mode: "0755"
  delegate_to: localhost
  run_once: true

- name: Cache node_exporter binary on the controller
  get_url:
    url: "{{ node_exporter_url }}"
    dest: "{{ node_exporter_cache_path }}"
    mode: "0644"
  delegate_to: localhost
  run_once: true

- name: Upload node_exporter binary to the host
  copy:
    src: "{{ node_exporter_cache_path }}"
    dest: "{{ bin_path }}"
    mode: "0755"
  notify: Restart node_exporter

- name: Set ownership on node_exporter directory
  file:
    path: "{{ node_exporter_path }}"
    state: directory
    owner: "{{ service_user }}"
    group: "{{ service_user }}"
    recurse: yes

- name: Deploy systemd service file
  template:
    src: node_exporter.service.j2
    dest: "{{ service_file_path }}"
    mode: "0644"
  register: service_file_deployed
  notify: Restart node_exporter

- name: Reload systemd daemon
  systemd:
    daemon_reload: yes
  when: service_file_deployed.changed

- name: Enable and start service (first run)
  systemd:
    name: node_exporter
    state: started
    enabled: yes
  when: not service_file.stat.exists

- name: Wait for node_exporter to come up
  wait_for:
    host: localhost
    port: "{{ node_exporter_port }}"
    state: started
    timeout: 15

- name: Health check
  uri:
    url: "http://localhost:{{ node_exporter_port }}/metrics"
    status_code: 200
