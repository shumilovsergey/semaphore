[Unit]
Description=Loki
After=network.target

[Service]
Type=simple
User={{ service_user }}

WorkingDirectory={{ loki_path }}
ExecStart={{ bin_path }} \
  -config.file={{ config_path }}

Restart=on-failure
RestartSec=5
LimitNOFILE=10000

[Install]
WantedBy=multi-user.target
