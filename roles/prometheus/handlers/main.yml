- name: Restart prometheus
  systemd:
    name: prometheus
    state: restarted

- name: Reload prometheus config
  uri:
    url: "http://localhost:{{ prometheus_port }}/-/reload"
    method: POST
    status_code: 200
